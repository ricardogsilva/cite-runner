# OGC-CITE Runner - how to run it standalone

Install from PyPI:

```shell
pipx install ogc-cite-runner

# can also use pip (preferably in a virtualenv)
python3 -m venv .ogc-cite-runner-venv
source .ogc-cite-runner-venv/bin/activate
pip install ogc-cite-runner
```
